CLAUDE.md: math232-fa26

This file is the baseline reference for working on this project. It is a living document: extend or revise it as conventions solidify, as new content types (notes, handouts, homework, slides, syllabus) get their first real implementation, or as the activity/objectives conventions themselves evolve. Do not treat anything here as more fixed than the project actually is. When in doubt, ask rather than assume.

Writing style

Do not use em dashes in any generated text, whether in this file, in PreTeXt source content, or in conversation. Write separate sentences instead. If an em dash is genuinely needed inside PreTeXt document content (not this file), use the PreTeXt markup element `<mdash/>` rather than a literal em dash character or an escaped character code.

Note on existing content: the activities authored so far (`02-derivative-graph-shape.ptx`, `03-accumulated-change-and-area.ptx`, `05-riemann-sum-approximation.ptx`, `06-riemann-sum-notation.ptx`) use the literal entity `&#8212;` in several places rather than `<mdash/>`. That predates this rule. Leave those instances alone unless asked to fix them, but use `<mdash/>` in anything written going forward.

Operational boundaries

* Claude's job here is to create and edit PreTeXt source documents (`.ptx` files and related assets). Claude does not run `pretext build`, `pretext view`, `pretext deploy`, or any other `pretext` command. The user runs all such commands themselves.
* Claude does not run any git commands (`add`, `commit`, `push`, etc.). The user handles all version control manually.
* The user may ask for guidance on what command to run or how to proceed in a given situation. That is a request for advice the user will act on themselves. It is never permission for Claude to execute the command.

Project overview

This is a PreTeXt course project ("Calculus with Functions II", MATH 232), originally scaffolded with `pretext new course`. The course is assembled as a single PreTeXt `<book>` in `source/main.ptx`, which pulls in every other source file via `<xi:include>` and organizes them into chapters (`course-documents`, `course-notes`, `activities`, `handouts`, `homework`, ...).

When a new activity or notes file is added, `source/main.ptx` needs a corresponding `<xi:include>` added inside the relevant `<chapter>`. Follow the existing include list as the pattern; it's currently manually curated, in the order the files should appear.

Repository map

* `source/main.ptx` is the book assembly file. It is the source of truth for what's included and in what order.
* `source/docinfo.ptx` and `source/frontmatter.ptx` hold shared macros and metadata, included at the top of the book.
* `source/activities/` holds in-class worksheets. Established conventions; see below. This is currently the only content type in the course with real, worked-out conventions.
* `source/notes/` holds weekly outlines (`week01.ptx`, `week02.ptx`, ...), one `<section xml:id="notes-week-NN">` per week, titled `"Week N"`, with a `<subsection>` per class meeting day (e.g. Monday/Wednesday/Friday). Unlike the parallel math231 project, where the analogous file (there called "objectives") already has a settled, worked-out convention, the notes files here are still just the unfilled `pretext new course` stubs: empty `<p/>` placeholders under each day, no learning-outcome structure, no sample exercises. Do not treat the current stub structure as a model to imitate. When notes-writing becomes a real, active task, work out the actual content conventions with the user fresh, and update this file accordingly.
* `source/handouts/`, `source/homework/`, `source/slides/`, and `source/syllabus/` are present only as the unmodified stubs/templates generated by `pretext new course`. No real conventions exist for these yet. Do not treat their current contents as a model to imitate. When one of these becomes a real, active task, its conventions should be worked out fresh, and this file updated accordingly.
* `assets/images/` holds supporting images referenced by activities (e.g. `Day1_Activity_RateGraph.png`, a graph handed out the class before an activity that references it).

Activity (worksheet) design philosophy

Activities are built as `<worksheet>` documents, run as genuine team-based (POGIL-style) group work, not solo take-home worksheets. Each activity's introduction typically assigns each group member one of four roles for the session:

* Manager: keeps the group on task and on pace, makes sure everyone has spoken before the group moves on, asks the instructor for help if the group is stuck after trying on its own.
* Recorder: writes the group's agreed-on answers in the group's own words, notes any question the group could not resolve.
* Spokesperson: speaks for the group during whole-class discussion and can explain why the group answered as it did, not just what was written.
* Reflector: checks in periodically on whether everyone understands, not just whether the group is moving forward, leads a short end-of-activity check-in on what worked. In a group of three, the Manager also takes on the Reflector's duties.

This role table (as a `<tabular>`) appears in the introduction of activities that introduce or remind students of the format; once the routine is established, later activities may skip re-stating it. Whether to include it on any given activity is a live teaching call, not a fixed rule.

As with any team task, the framing is that the goal isn't to race to an answer, it's for everyone in the group to be able to explain why an answer is correct, not just to have written one down.

Time and pacing considerations carry over from the same real-classroom constraints as other activity-based courses: content that feels like a modest amount of material tends to fill the whole class period once group work and whole-class discussion are folded in. Design conservatively rather than assuming discussion time is separate from the content's own footprint.

That said, POGIL's default pace, one concept, thoroughly discovered, per class, is not sustainable across an entire course with a fixed number of class periods; strict adherence to it will not finish the syllabus. Multiple related sub-skills routinely need to be packed into a single day's activity. In practice this means:

* Give some results as stated facts (a `<definition>` or a reminder `<remark>`) rather than having the group discover everything for itself, especially for a second or third instance of a pattern already established once. For example, if an activity asks students to derive two similar results by the same method, it is fine to derive the first in full and then just state or lightly re-derive the second, rather than repeating the full derivation.
* Use a suggested-pacing comment (per-exercise timing estimates) and an explicit "where to cut if you're running short" list, in priority order, as standard practice on every activity, not only when pacing looks tight. Follow the pattern already established in `07-summation-formulas.ptx` and `08-definite-integral-as-limit.ptx`.
* Treat "we didn't finish" as a normal, expected outcome to design around, not a failure of the activity. When writing the next activity in sequence, check where the class actually stopped and write that activity to pick up from there, rather than assuming the previous one was completed in full. If the previous activity's ending content wasn't reached, it is fine for the next activity to include it (even though it also still lives, unreached, in the earlier file) rather than leaving a gap.

Activity XML conventions

Based on the existing activities (`02-derivative-graph-shape.ptx`, `03-accumulated-change-and-area.ptx`, `04-properties-of-the-integral.ptx`, `05-riemann-sum-approximation.ptx`, `06-riemann-sum-notation.ptx`):

* Root element is `<worksheet xml:id="activity-<descriptive-slug>">`. The `xml:id` is a descriptive slug naming what the activity is about (e.g. `activity-derivative-graph-shape`), not just a number. File names carry the numeric ordering prefix (e.g. `06-riemann-sum-notation.ptx`); the `xml:id` does not need to repeat it.
* Immediately after `<title>`, an `<objectives xml:id="obj-<same-slug>">` element lists the activity's learning objectives as a `<ul>` of `<li><p>...</p></li>` items. This is a required, established part of the pattern for every real activity so far (all of 02 through 06 have one; only the barebones `01-intro-activity.ptx` and the template lack it). Objective statements describe what the student should be able to do by the end of the activity, and often name the specific skill being layered on (e.g. "use the slope of the graph of f' to determine the sign of f''").
* A top-level `<introduction>` sets up the activity's premise, and is also where the team-roles table lives when it's included (see above).
* Each `<exercise>` has a descriptive `<title>` naming what that phase of the activity is about (e.g. "What Does the Graph of the Rate Tell Us?", "Sketching the Shape of f"). Unlike a numbered "Section N: ..." labeling scheme, these titles stand on their own.
* An `<exercise>` typically carries `workspace="0in"` at its own level (the exercise's own introduction is where formalization/naming happens, not blank workspace) with the actual writing room distributed across its `<task>` elements.
* `<task xml:id="task-...">` elements are the sub-steps within an exercise, each with its own `workspace` sized to the work expected: commonly `1in` to `1.5in` for a focused question, up to `2in` for sketching a full graph.
* Task statements are written as prose paragraphs posing a question or asking students to do/explain something, not as `<ul>`/`<ol>` step lists (though a handful of activities do use short lists where a genuinely enumerable set of steps calls for it).
* Inline markup conventions in use: `<m>`/`<md>` for math, `<em>` for emphasis, `<image source="images/....png" width="..."/>` for a supporting graph or figure carried over from a prior class's handout.
* `<latex-image>` (with a `<shortdescription>`, tikz/pgfplots code, wrapped as needed) is used in some activities (`03`, `04`, `05`) for custom-drawn figures. Follow the existing examples in those files as the pattern rather than treating any one as canonical, since this is still a developing convention.

Notes for future updates to this file

* Notes (`source/notes/`), handouts, homework, slides, and syllabus content are all still unauthored beyond their generated stubs. When any of these gets its first real authoring pass, work out its conventions with the user the same way activities were worked out here, then add a section for it above.
* If the activity role-table convention, the exercise/task/objectives mapping, or the decision to keep or retire the literal `&#8212;` instances in early activities changes, update the relevant section rather than leaving this file stale.
